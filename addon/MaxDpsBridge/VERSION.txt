1.3.6
date: 2026-09-24
git: <commit placeholder — fill at release>
interface: 120100 (Retail Midnight 12.1)
protocol: v4 (9 cells; status cell B = flags bit0 in-combat, bit1 on-GCD, bit2 has-target)
behaviour: v1.3.6 — out-of-combat toggle OFF ⇒ hard pause until in combat; ON ⇒ out-of-combat attack allowed but WAITS for a target (no spam into empty space)
combat-gcd: v1.3.5 — companion waits for GCD (interrupt bypasses), presses in the availability window
interrupts: v1.3.5 — category read from MaxDps.classInterrupts (exact), not dirty-set membership (arg-blind hooks broke it)
slots: Main UNGATED (upstream glow IS the verdict) + SpellsGlowing-first pick; Offensive/Defensive/Consumable/Trinket/Interrupt-last keep the readiness gate
bindings: overlay HotKey (dropsecretaccess-contained, miss-tolerant) → overlay bindText → bar scan → texture map
readiness: v1.3.0 ZERO-TAINT (issecretvalue NEVER called; arg-blind hooks + ScrubbedFlags; CooldownConsolidated.scrubbed-ready, NeverSecret flags, Duration objects, dropsecretaccess(); NO UnitCastingInfo; NO raw C_Spell arithmetic)
slots: Main / Offensive / Interrupt / Defensive / Consumable / Trinket (Spell Frame naming)
upstream: MaxDps v11.3.43 (Interface 120100, read at runtime, never bundled)
