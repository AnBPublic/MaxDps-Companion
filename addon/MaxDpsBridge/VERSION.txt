1.3.9
date: 2026-09-24
git: <commit placeholder — fill at release>
interface: 120100 (Retail Midnight 12.1)
protocol: v4 (9 cells; status cell B = flags bit0 in-combat, bit1 on-GCD, bit2 has-target)
perf: v1.3.9 — per-tick memo (MDB.BeginTick): one scrubsecretvalues(Flags) + one ItemSpells map + one class/spec resolve per tick instead of ~10/~4/~6; Duration evaluation curve built once and reused (was ~6 curves/tick); no per-slot closures in the readiness gate; strip refresh 50 ms -> 33 ms (30 Hz) for lower action latency at lower total cost than the old 20 Hz path
behaviour: unchanged from 1.3.6/1.3.7 — main/off/def/cons/trin/int, GCD + target + combat gates, interrupts execute
